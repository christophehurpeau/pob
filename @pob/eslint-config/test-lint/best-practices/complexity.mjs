// eslint-disable-next-line complexity
export function a(x) {
  // eslint-disable-next-line unicorn/prefer-switch
  if (x === 1) {
    return x; // 1st path
  } else if (x === 2) {
    return x + 1; // 2nd path
  } else if (x === 3) {
    return x + 2; // 3rd path
  } else if (x === 4) {
    return x + 3; // 4th path
  } else if (x === 5) {
    return x + 4; // 5th path
  } else if (x === 6) {
    return x + 5; // 6th path
  } else if (x === 7) {
    return x + 6; // 7th path
  } else if (x === 8) {
    return x + 7; // 8th path
  } else {
    return 9; // 9th path
  }
}

export function b(x) {
  // eslint-disable-next-line unicorn/prefer-switch
  if (x === 1) {
    return x; // 1st path
  } else if (x === 2) {
    return x + 1; // 2nd path
  } else if (x === 3) {
    return x + 2; // 3rd path
  } else if (x === 4) {
    return x + 3; // 4th path
  } else if (x === 5) {
    return x + 4; // 5th path
  } else if (x === 6) {
    return x + 5; // 6th path
  } else if (x === 7) {
    return x + 6; // 7th path
  } else {
    return 8; // 8th path
  }
}
