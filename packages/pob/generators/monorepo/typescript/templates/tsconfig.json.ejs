{
  "files": [],
  "references": [
<% packageNames.forEach((packageName, index) => { -%>
    { "path": "packages/<%= packageName %>" }<%= index === packageNames.length -1 ? '' : ',' %>
<% }) -%>
  ]
}
