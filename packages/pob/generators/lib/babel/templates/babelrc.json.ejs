{
<% if (hasReact) { -%>
  "presets": [
    "pob-react"
  ],
<% } -%>
  "env": {
<% if (testing) { -%>
    "test": {
      "only": ["src/"],
      "presets": [
        ["pob-babel/preset"]
      ]
    }
<% } -%>
  }
}
