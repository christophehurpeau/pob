{
  "include": [
<% workspaces.forEach((workspace, i) => { -%>
    "packages/*/src/**/*.ts<%= jsx ? 'x?' : '' %>"<% if (i + 1 !== workspaces.length) { %>,<% } -%>
<% }) %>
  ],

  "exclude": [
    "**/node_modules/**",
    "**/src/**/*.test.ts<%= jsx ? 'x?' : '' %>",
    "**/src/**/__tests__/**"
  ],

  "compilerOptions": {
    "target": "esnext",
    "module": "esnext",
    "lib": ["dom", "esnext"],
    "moduleResolution": "node",
    "esModuleInterop": true
  },

  "typedocOptions": {
<% /* if (useYarn2) { -% >
    "theme": ".yarn/unplugged/typedoc-neo-theme-npm-1.0.10-ef449bda35/node_modules/typedoc-neo-theme/bin/default",
< % } else { -% >
    "theme": "./node_modules/typedoc-neo-theme/bin/default",
< % } */ -%>
    "entryPoints": [
      <%- packageNames.filter(n => !n.endsWith('-example')).map(n => n.startsWith('@') ? `"${n}/src/index.ts"` : `"packages/${n}/src/index.ts"`).join(', ') %>
    ],
<% /*
    "links": [
      {
        "label": "GitHub",
        "url": "< %- repositoryUrl % >/nightingale"
      }
    ],
    "source": [
      {
        "path": "< %- repositoryUrl % >/tree/master/packages/",
        "line": "L"
      }
    ],
*/ -%>
    "readme": "README.md",
    "out": "docs",
    "excludePrivate": true,
    "gitRevision": "master"
  }
}
