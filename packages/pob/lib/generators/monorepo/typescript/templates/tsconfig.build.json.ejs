{
  "files": [],
  "references": [
<% packagePaths.forEach((packagePath, index) => { -%>
    { "path": "packages/<%= packagePath %>/tsconfig.build.json" }<%= index === packagePaths.length -1 ? '' : ',' %>
<% }) -%>
  ]
}
