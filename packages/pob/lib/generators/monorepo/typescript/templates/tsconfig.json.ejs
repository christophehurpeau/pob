{
  "files": [],
  "references": [
<% packagePaths.forEach((packagePath, index) => { -%>
    { "path": "<%= packagePath %>/tsconfig.json" }<%= index === packagePaths.length -1 ? '' : ',' %>
<% }) -%>
  ]
}
