{
  "files": [],
  "references": [
<% packageNames.forEach((packageName, index) => { -%>
    { "path": "packages/<%= packageName %>/tsconfig.json" }<%= index === packageNames.length -1 ? '' : ',' %>
<% }) -%>
  ]
}
